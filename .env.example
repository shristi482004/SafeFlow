# SafeFlow Environment Variables

# Port the server listens on (default: 8080)
PORT=8080

# Google Gemini API key
# Get yours from https://aistudio.google.com/
# If not set, the app runs on the offline mock engine — fully functional for demos and testing.
GEMINI_API_KEY=
